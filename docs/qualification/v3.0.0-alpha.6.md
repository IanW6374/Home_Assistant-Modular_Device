# IoT-MD v3.0.0-alpha.6 qualification

Status: **awaiting hardware evidence**.

The source, contract and host suites validate the qualification recorder,
cutover refusal, persistence, sticky failures, release isolation, offline-probe
semantics and compatibility fallback. They do not mark physical tests complete.

The on-device portal is the live operator view. The exported evidence is the
machine-readable record. The `v3-beta` profile requires a 48-hour soak with at
least 2,400 health and storage observations, three bounded network recoveries,
one managed renewal, three confirmed paired upgrades, three power recoveries,
three native recovery exercises, three watchdog recoveries, identity and fleet
interoperability, migration rollback, and all 13 driver variants. The minimum
sample counts prevent one late observation from qualifying the entire soak.

Record actual outcomes only. Do not record a successful event merely to make a
counter advance. This page will be updated with the evidence file and hardware
identity after the campaign completes.
