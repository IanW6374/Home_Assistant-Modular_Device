wifi_ssid = "your-ssid"
wifi_password = "your-wifi-password"

mqtt_server = "mqtt.example.local"
mqtt_username = "mqtt-user"
mqtt_password = "mqtt-password"
mqtt_ssl = True

web_portal_token = "replace-with-a-long-random-url-safe-token"

# Dedicated password for the temporary Wi-Fi recovery access point (8+ characters).
# If omitted, the firmware falls back to web_portal_token.
recovery_ap_password = "replace-with-another-long-random-password"
